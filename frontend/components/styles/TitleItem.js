import styled from 'styled-components';

const TitleItem = styled.h3`
  margin: 5px 0;
  line-height: 1.2;
  font-size: 1.5rem;
  padding: 0 1rem;
  a {
    display: inline;
    color: var(--darkGreen);
  }
`;

export default TitleItem;
